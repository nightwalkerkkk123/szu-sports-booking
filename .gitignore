# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/

# Testing
.pytest_cache/
.coverage
htmlcov/
*.cover

# Logs
logs/
debug/
data/*.db

# User-specific runtime state (session cookies, rate-limit state)
# Never commit these — they contain active auth tokens.
data/cookies/
data/rate_limits.json

# HAR captures (large + may contain session cookies / PII)
*.har

# Environment
.env

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build
dist/
build/
*.egg-info/

# Go binaries
surf-proxy/surf-proxy
*.exe
# Temporary scratch space (one-off patch scripts, debug helpers, dumps)
# Keep local-only; clean periodically with make tmp-clean or git clean -fX tmp/.
tmp/

