[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -m "not real_env" --tb=short
markers =
    unit: Unit tests
    integration: Integration tests
    smoke: Smoke tests
    real_env: Real environment tests (requires env vars)
    network: Tests that require outbound network access